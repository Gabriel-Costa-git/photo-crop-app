name: photo_crop_app
description: App simples para cortar fotos
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  image: ^4.1.3
  image_gallery_saver: ^2.0.3
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  google_mobile_ads: 3.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
